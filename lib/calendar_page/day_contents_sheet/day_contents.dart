import 'package:flutter/material.dart';

class DayContents extends StatelessWidget {
  const DayContents({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
